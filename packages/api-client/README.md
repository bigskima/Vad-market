# @vad/api-client

Shared transport concerns—authorization headers, request IDs, idempotency keys,
and the standard error envelope. It deliberately contains no eligibility,
pricing, fee, wallet, settlement, or provider-selection logic.
