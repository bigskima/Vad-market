name: Add Dynamic native peer dependency

on:
  push:
    branches: [dynamic-email-wallet]
    paths:
      - .github/workflows/add-dynamic-native-peer.yml

permissions:
  contents: write

jobs:
  add:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dynamic-email-wallet
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Install Dynamic native peer
        run: npm install react-native-passkey --workspace @vad/mobile
      - name: Commit dependency metadata
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/mobile/package.json package-lock.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Install Dynamic native passkey dependency"
          git push origin HEAD:dynamic-email-wallet
