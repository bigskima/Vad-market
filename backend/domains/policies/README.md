# Policies

Owns versioned, effective-dated business configuration and feature flags.
Policy history is append-only; clients consume evaluated capabilities and never
promote a denied capability locally.
