# Identity

Owns authenticated account state and the separation between public profiles and
private identity data. Supabase Auth establishes identity; RLS and RBAC establish
authorization. Client-editable user metadata is never an authorization source.
