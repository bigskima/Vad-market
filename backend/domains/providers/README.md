# Providers

Owns provider capabilities, routing priority, environment, and health. Domain
code depends on provider interfaces rather than vendor-specific names. Database
records may reference secrets, but never contain credentials.
